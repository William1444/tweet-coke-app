# AngularJS Tweet Coke Application

## Overview

This is an application that fetches tweets from a Coke tweet service and displays them.

## Prerequisites

### Node.js and Tools

- Get [Node.js][node-download].
- Install the tool dependencies (`npm install`).

## Workings of the application

- The application filesystem layout structure is based on the [angular-seed] project.

## downloading source and installing dependencies

The application relies upon various node.js tools, such as Bower, Karma and Protractor.  You can
install these by running:

```
#clone git project

git clone https://github.com/William1444/tweet-coke-app.git

cd tweet-coke-app

#
npm install

### run the application

npm start

navigate to http://localhost:8000/app/index.html

### run the tests

navigate to http://localhost:8000/test/specrunner.html

